# 交付验证记录

检查日期：2026-09-22。此记录区分已运行的检查与仍需账户/基础设施的验收。

## 已完成

- PHP 8.3.33 官方 Windows 便携运行时，下载后校验发布方 SHA-256；自有 PHP 文件语法检查通过。
- WordPress 7.1.1 + WooCommerce 11.1.1，使用官方 SQLite Database Integration 3.0.2 建立隔离本地验证环境。SQLite 仅用于本地验证；另有下述 Docker/MariaDB CI 验证。
- 演示导入执行两次，同一 SKU 没有重复，既有页面/商品不会被覆盖。
- 24 项真实 WordPress/WooCommerce 业务断言通过：普通配件购买、整车询价限制、变体继承、直接加购拒绝、结构化数据、输入校验、私有数据权限、四语言翻译加载等。
- HTTP 首页、商城、联系、品牌故事返回 200；不存在页面返回 404。
- HTTP 询价：错误 nonce 403、无效数量/未同意隐私 422、蜜罐 400、合法请求保存后跳转成功、重复请求 429。
- 四个翻译目录各 100 条字符串完整性、占位符和二进制 MO 读取校验通过。
- Playwright + Microsoft Edge 检查真实首页 1440px / 390px：无水平溢出、移动导航可以打开并用 Escape 关闭、页面无 JavaScript 异常；390px 商城无水平溢出。桌面和移动整页截图已导出。
- 浏览器验证整车详情没有购买按钮，询价按钮带入正确商品；配件加入购物车后会话保存产品与 399 USD 演示金额，原生区块结账显示同一商品；390px 结账无水平溢出。测试等待 WooCommerce 区块完成客户端加载后再断言，未尝试真实付款。

本地初始化曾因自制 CLI 引导器缺少 WP-CLI 的依赖类失败，已改为官方 WP-CLI 2.12.0 完成安装；固定链接和遗漏译文已修复。该临时引导器不在仓库交付中。主题切换命令触发过 WooCommerce 早期翻译加载 notice，后续 HTTP 检查未观察到致命错误。

## 自动化与待验收项

初始实现提交 `f032bffbb95fff8506fc7735f6780474f54349f5` 的 [GitHub Actions 检查已通过](https://github.com/yueyuefeng/WholeSaleWeb/actions/runs/35682129337)：Docker/MariaDB 启动、安装 WordPress/WooCommerce/Stripe/PayPal/Yoast、两次 seed、24 项业务断言、翻译完整性、HTTP 页面与询价流程均成功。后续提交若仅更新本记录，以各自 GitHub Actions 结果为准。插件成功安装不代表账户级支付联调已经完成。

尚未完成：真实域名与 HTTPS、生产主机部署、WPML 商业插件联调、五语言全部商品和政策的人工审校、WCML 汇率/多币种订单、Stripe/PayPal 商户连接与支付退款、SMTP 实际送达、真实物流/税费、生产恢复演练、Core Web Vitals 实测。

这些步骤需要域名/主机、许可证、真实商品数据、明确销售国家和商户主体。尚未启用真实收款或公开站点索引。所有演示商品、价格、插画和政策草稿须替换后才可正式销售。

## 视觉升级复验（1.1.0）

按照品牌方反馈，改为深色科技风、五张本地 WebP 摄影，并将完整中文价值主张放在首屏 H1。更新摄影来源和许可证记录。四套翻译各含 109 条文案，当前源码使用的 93 条均通过编译与覆盖校验（保留旧版未使用文案以兼容）。

Playwright + Edge 检查 1440px / 390px 首页及 390px 商城，无水平溢出、无 JavaScript 错误；移动菜单开合和 Escape 关闭正常。检查首屏照片、全部分类照片与改装特写实际加载。详情及购物车/结账的既有业务逻辑保持原样，仍需商业账户联调后才能真实收款。

## 价值主张多语言复验（1.1.1）

首页 H1 从固定中文改为随站点语言显示，五种文案按“无惧艰险、勇往直前、不虚此生”语义转写；中文原句完整保留。移除重复释义段落，H1 继承页面语言，长句采用响应式排版。

- 上一版摄影与深色主题提交 `0965c8f7ede3a6cce4ddc203269da20f5f3b5b57` 的两次 GitHub Actions 检查均成功；人工复查移动端结账国家/州选择框深色样式可读。
- 四套翻译各 112 条，源码当前使用的 95 条全部通过覆盖、占位符及 MO 二进制读取检查。
- 新增 `tests/manifesto.php` 并接入 CI，实际渲染首页并断言五种语言的 H1 文案与无固定中文 `lang` 属性。
- 本地安装官方 WordPress 核心语言包后逐个切换站点语言，浏览器检查 en_US、zh_CN、de_DE、fr_FR、es_ES 在 1440、390、320px 共 15 种组合；H1 译文、页面语言、无横向溢出、按钮不重叠均通过，无 JavaScript 异常。完成后恢复默认英语。
- 此验证使用 WordPress 站点语言，未安装商业 WPML；WPML 页面切换及商品翻译仍属于正式部署联调事项。

## 联系页聊天复验（1.2.0）

联系入口改为直接聊天，详细询价保留在折叠区。增加非公开会话/消息存储、WordPress 后台人工回复、访客轮询、人工接管、删除和过期清理；机器人提供服务端过滤器和 HTTPS 适配协议，当前未配置真实模型账户。

- `tests/chat.php` 的 27 项断言通过，覆盖独立密钥及哈希存储、跨会话读写/删除拒绝、输入限制、并发锁、重试去重、消息保存、禁止缓存、客服权限与回复、机器人角色/语言、人工接管、故障降级、HTTPS 协议及密钥隔离、限流、失效和删除。
- `tests/chat-http.py` 验证真实 HTTP 的 nonce、正确本地端口 Origin、外站拦截、发送、历史恢复、幂等、转人工与删除。
- Playwright + Edge 验证前台直接发消息、刷新恢复、独立标签页没有他人消息、真实 `shop_manager` 登录后台发送回复后访客自动收到、网络中断草稿保留与重试、390/320px 无横向溢出、删除和中文聊天界面。测试账户与会话执行后删除。
- 四套翻译各 158 条，源码使用的 141 条全部通过覆盖和 MO 编译一致性检查。
- 浏览器联调发现 WordPress 通用 Origin 列表不保留本地端口，已改为按站点实际 scheme/host/port 验证。补充防止过时轮询结果覆盖删除/新会话，以及浏览器后退恢复轮询。

真实机器人供应商联网调用、持续客服值守、外部通知、生产调度与反向代理/CDN 排除规则仍需在上线配置后验收。HTTPS 适配测试采用模拟 HTTP 响应，未消耗模型额度。

## 选购工具复验（1.3.0，2026-09-23）

- 新增 48 项真实 WP/WooCommerce 断言：数值和数据结构校验、审核来源与续航条件、初步匹配/不匹配/未知、演示强制待确认、自行车资料、配置依赖/互斥、非法国家/数量/商品、CSRF/Origin、分享码哈希、方案过期/下架/资料变化、聊天拒绝伪造上下文及同品类比较。
- 原有 24 项交易/询价业务、27 项聊天、五种语言首页价值主张断言回归通过；PHP 语法、前端脚本语法与四语言覆盖检查通过。四套目录各 242 条，当前源码 225 条。
- 新增真实 HTTP 测试：配置冲突、保存、no-store/noindex 响应头、分享恢复和带方案建聊通过；聊天 HTTP 与页面/询价 HTTP 回归通过。
- Playwright + Edge 跑通商品详情 → 配置冲突 → 正确配置 → 保存/恢复 → 带方案聊天并确认机器人上下文，兼容未知项交人工，三款同类对比、跨类拒绝；1440/390/320px 无整页横向溢出、无 JavaScript 异常，中文 UI 正常。比较表格允许局部横向滚动。
- CI 已纳入 `tests/selection.php` 和 `tests/selection-http.py`，Docker/MariaDB 运行结果以该提交的 GitHub Actions 为准。

配置器目前保存询价偏好，不计算报价、锁库存或生成订单；兼容工具只有初筛能力。真实 SKU/规则、商业 WPML、多国家支付退款、物流与实际 AI 服务仍未验收。详见 [选购工具说明](11-selection-tools.md)。

